CREATE TABLE [dbo].[Institution] (
    [ID]              INT            NOT NULL,
    [InstitutionType] INT            NOT NULL,
    [Name]            NVARCHAR (100) NOT NULL,
    [NameInLatin]     NVARCHAR (100) NOT NULL
);

